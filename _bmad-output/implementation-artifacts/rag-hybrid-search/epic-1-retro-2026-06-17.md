# Epic 1 回顾: RAG 检索基础设施升级 — 扩展点与配置化

- **日期**: 2026-06-17
- **项目**: my-AI
- **参与者**: spike (Project Lead), Amelia (Developer Facilitator)
- **类型**: 首次回顾

---

## Epic 1 交付指标

| 指标 | 值 |
|------|-----|
| 完成故事 | 6/6 (100%) |
| 覆盖功能需求 | FR-1 ~ FR-6 |
| 测试数 | 8 → 42+ (AskQuestion: 11, RuleBasedClassifier: 31) |
| 生产事故 | 0 |
| 技术债务项 | 2 (deferred work) |

## 故事交付概览

| 故事 | 标题 | 审查修复项 | 备注 |
|------|------|-----------|------|
| 1.1 | RetrievedChunk score 字段 | 7 项 | NaN/Infinity 防御、Javadoc、测试改进 |
| 1.2 | RerankingPort 可插拔接口 | 3 项 | Javadoc 一致性 |
| 1.3 | 检索参数配置外部化 | 6 项 | 前缀不匹配、@Max 约束、缺失 validation 依赖 |
| 1.4 | QueryType + QueryClassifierPort | 0 | 纯新增 domain 文件，零修复 |
| 1.5 | RuleBasedQueryClassifier | 0 | 纯新增实现 + 测试，零修复 |
| 1.6 | CHITCHAT 拦截集成 | review 完成 | 构造器 7→9 参数 |

## 做得好的

1. **递增依赖链执行干净** — 6 个故事严格按顺序交付，每个故事的前置上下文正确传递，没有返工
2. **纯新增故事零修复** — 1.4 和 1.5 一次通过，domain 层设计模式已稳定
3. **测试增长健康** — 每个故事都有新增测试覆盖
4. **六边形架构零违反** — 全程合规

## 需要关注的

1. **审查修复量大（4/6 故事）** — 主要集中在 Javadoc 不完整、边界防御缺失、配置前缀不一致
   - 根因：实现时对 Javadoc 和校验的关注不够系统化
2. **构造器参数膨胀（7→9）** — AskQuestionApplicationService 已到可读性临界点
3. **依赖遗漏** — `spring-boot-starter-validation` 在编译时才发现缺失

## 技术债务

| 来源 | 项 | 说明 |
|------|-----|------|
| Story 1.1 review | record equals/hashCode 变更风险 | 外部用 Set/Map 存储 RetrievedChunk 时行为差异，预存风险 |
| Story 1.1 review | score 0.0 默认值语义模糊 | "未计算"与"零置信度"难区分，需 spec 更新才能改 |
| Story 1.3 review | 应用层 import infrastructure 层 | 既存模式，非新引入 |
| Story 1.3 review | 双重默认值来源 | Java 字段默认 + YAML 默认相同，维护不一致风险 |

## 行动项

| # | 行动项 | 说明 | 时间 |
|---|--------|------|------|
| 1 | Javadoc 清单化 | 新文件实现后提交前自检 `@author + @since` | Epic 2 起持续 |
| 2 | 构造器参数封装 | Epic 2 如需再加参数，考虑 Dependencies record 封装 | Epic 2 评估 |
| 3 | Story spec 依赖检查 | 新增 `@ConfigurationProperties` 时显式检查 validation 依赖 | Epic 2 spec 编写时 |

## Epic 2 准备评估

### Epic 2 概览: Hybrid Search 多路融合检索

4 个故事: Flyway V9 tsvector → ScopeFilterBuilder → SparseRetrievalAdapter BM25 → HybridChunkRetrievalAdapter RRF 融合

### 对 Epic 1 的依赖

- ✅ RetrievedChunk.score（1.1）— Sparse 路径也需填充 score
- ✅ 检索参数配置化（1.3）— retrievalTopK 计算已外部化
- ✅ CHITCHAT 拦截（1.6）— Hybrid 切换后 CHITCHAT 仍需跳过检索

### 技术风险

1. Flyway V9 是 DB schema 变更，需本地 PostgreSQL 环境
2. CompletableFuture 并行 + 降级策略，测试复杂度高于 Epic 1
3. ScopeFilterBuilder 重构现有 PgVectorChunkRetrievalAdapter，有回归风险

### 准备清单

- [x] Epic 1 全部 6 故事完成
- [x] 本地 PostgreSQL + PGVector 环境就绪（Epic 2 需要 Flyway 迁移验证）
- [x] 确认 vector_store 表当前数据量（V9 回填性能评估）
