# my-AI 仓库上下文

## 1. 文档目的

本文件用于说明 `my-AI` 仓库当前稳定的业务语义、系统边界、核心术语和决策约束。

目标读者：

- 未来重新接手这个仓库的自己
- 参与该仓库的协作者
- 需要先理解领域语言再开始分析或改造代码的 agent / tooling

本文件不替代详细设计、运行手册或版本计划；它只描述“这个仓库长期成立的上下文”。

## 2. 仓库定位

`my-AI` 是一个基于 Spring Boot + Spring AI 的文档入库与检索问答项目。

当前主线目标不是一次性做成完整 SaaS 平台，而是按阶段演进，先把下面这条链路做稳：

`上传文档 -> 可追踪处理 -> 可检索 -> 可问答 -> 可治理`

当前仓库已经从早期的最小 RAG 闭环，演进到包含以下能力的单工作区系统：

- 文档上传、处理、重处理、删除
- 知识库主数据管理
- 基于知识库的问答
- 单工作区下的认证、授权和审计基础能力
- 前后端一体的本地演示与联调链路

## 3. 当前阶段

当前应把本仓库理解为：

- **单工作区优先** 的内部协作型系统
- **RAG 主链路已跑通**，正在持续增强解析质量、治理能力和工程稳定性
- **V1.1 管理基线已形成**，后续重点会继续落在解析增强、权限完善和更真实的业务场景支持上

不要把它理解成：

- 已完成多租户 SaaS 的成熟产品
- 已稳定支持所有复杂文档格式和 OCR 场景
- 已完成多模型、多供应商自由切换的平台

## 4. 领域主线

本仓库围绕三条主业务线组织：

### 4.1 ingest

负责文档资产的生命周期管理：

- 上传受理
- 状态追踪
- 解析与清洗
- 分块
- 向量入库
- 重处理
- 删除

这里的核心关注点是“文档能否被稳定处理并形成可追踪资产”，而不是回答生成本身。

### 4.2 knowledge

负责知识库主数据和可见性边界：

- 知识库创建、编辑、启停
- 知识库列表与统计视图
- 知识库作为上传与问答的主要业务边界

知识库是当前系统中最稳定、最自然的资源隔离单位。

### 4.3 qa

负责问答编排：

- 检索候选内容
- 构建问答输入
- 调用模型生成回答
- 返回结构化引用
- 默认只在当前选中的 `knowledge base` 内检索
- 默认只检索 `document` 的最新版本
- 当最新版本尚未 `INDEXED` 时，问答应继续检索该 `document` 最近一个已 `INDEXED` 的版本，直到新最新版本可检索
- 问答引用应显示来源版本
- 问答引用的来源版本信息应展示版本号与更新时间
- 答案正文不默认展示版本更新时间，仅在引用卡片中展示

`qa` 的职责不是拥有文档，而是消费 `ingest` 产出的可检索内容，并受 `knowledge` 和授权规则约束。

### 4.4 auth / governance

当前仓库已经有独立的认证与治理基础能力，虽然 README 仍以 `ingest / knowledge / qa` 为主线介绍，但分析现状时必须把认证和授权视为一等能力，而不是外围补丁：

- 本地账号登录
- 服务端 Session
- 工作区成员治理
- 知识库授权
- 文档授权覆盖
- 审计留痕

## 5. 核心资源与标识

理解本仓库时，优先使用下面这些术语，不要随意替换成语义接近但边界不同的词。

### 5.1 workspace

当前系统按 **单工作区** 优先设计，但 `workspace` 已经入模。

- 现在不要把它理解为“未来才会有的概念”
- 也不要把整个系统简单等同于“无边界的全局空间”
- `workspace` 是底层治理单元，主要承载成员归属、权限范围、审计范围与配置范围
- 普通用户在 UI 中只感知自己当前所属的 `workspace`，不需要看到其他 `workspace`
- 普通用户不能直接修改 `workspace`，`workspace` 相关变更只应由治理路径处理
- 当前用户模型按单归属处理：一个用户只隶属于一个 `workspace`

### 5.2 knowledge base

知识库是当前最主要的业务资源边界。

- 对外稳定标识是 `kbId`
- 文档上传、列表、问答、授权大多围绕知识库展开
- 不要把 knowledge base 混叫成“目录”“分组”“项目空间”
- 每个 `knowledge base` 只隶属于一个 `workspace`
- 上传文档时必须先选定 `knowledge base`，再创建或复用对应的 `document`
- 同一 `knowledge base` 下，同一内容哈希的上传应复用既有 `documentId`，文件名变化不影响是否复用
- 内容发生变化时，仍视为同一 `document` 的新版本演进，而不是自动生成新的 `documentId`
- 内容发生变化时，新版本归属不做隐式猜测；必须由用户从已有 `document` 显式发起“上传新版本”动作，并带入目标 `documentId` 作为上下文
- “上传新版本”入口只保留在文档详情页，不放在通用上传页或文档列表页
- “上传新版本”入口仅对具备目标 `document` 管理权限的用户显示
- 当前用户不具备“上传新版本”权限时，详情页隐藏该入口，不使用置灰展示
- “上传新版本”后端应提供独立接口，而不是复用通用上传接口；接口语义应绑定已有 `document` 上下文，例如 `POST /api/v1/documents/{documentId}/versions`
- “上传新版本”独立接口除文件本身外，不再额外要求业务字段；目标 `document` 上下文由路径参数承载
- “上传新版本”接口应携带 `expectedLatestVersionNumber` 一类乐观并发校验字段，用于防止用户基于过期详情页对旧最新版本发起新版本上传
- 从文档详情页发起“上传新版本”时，目标 `document` 所属 `knowledge base` 必须锁定，不允许在该流程中切换到其他 `knowledge base`
- “上传新版本”允许来源文件名变化，文件名变化不打断同一 `document` 的版本链
- 当前最新版本即使是“回退产生的版本”，后续仍允许继续作为“上传新版本”的基线版本
- 不允许从任意历史版本直接发起“上传新版本”形成分支版本链；若要基于旧版本继续演进，应先将其回退为最新版本，再从当前最新版本发起“上传新版本”
- 从文档详情页发起“上传新版本”时，若新文件内容与当前最新版本完全相同，则不创建新版本，按同内容复用处理
- 同内容未创建新版本时，详情页应明确提示用户未生成新版本，并说明当前仍停留在原最新版本
- 上传新版本成功创建新版本后，详情页默认应自动切换到新的最新版本视图
- 新版本创建成功后，详情页应通过页面内稳定结果提示承载结果与后续动作，不只依赖一次性成功消息
- 新版本创建成功后的稳定结果提示应同时展示新版本号与上一版本号
- 新版本创建成功后的稳定结果提示应提供“查看版本历史”快捷入口
- 新版本创建成功后的稳定结果提示可提供“去问答”快捷入口，但前提是当前存在可问答版本
- 当当前不存在任何已 `INDEXED` 的可问答版本时，新版本成功提示不显示“去问答”入口，并应明确提示“当前暂无可问答版本，请等待处理完成”
- 新版本创建成功后，不单独提供“继续查看当前版本详情”动作；当前详情页已默认停留在新最新版本视图
- UI 默认只展示 `document` 的最新版本，版本历史放在详情或治理页
- 旧版本仅对目标 `document` 具备管理权限的用户保留与可见，普通用户不可见
- `document` 软删除后，重新上传同内容应生成新的 `documentId`
- 删除确认必须明确告知：删除后同内容重新上传会生成新的 `documentId`
- 旧 `documentId` 入口失效等派生影响不进入删除确认主文案，可由次级说明或后续结果提示承接
- 删除确认主文案中的关键后果应拆成独立条目展示，不合并为一段长描述
- 删除确认与结果提示应直接使用 `documentId`、文档级授权等精确术语，不替换为模糊口语化说法
- 删除确认主文案应原样写出“同内容重新上传”，不省略该限定条件
- 删除操作应具备二次确认动作，不能只依赖一次点击确认
- 删除二次确认应要求用户输入并精确匹配完整 `documentId`
- 删除二次确认输入框必须保持为空，不得由路由参数、缓存或系统默认值预填，必须由用户手动输入完整 `documentId`
- 删除页应只读展示当前目标 `documentId`，作为用户手动输入二次确认时的对照信息
- 删除页展示的目标 `documentId` 不提供一键复制能力，避免削弱手动输入二次确认的保护作用
- 删除确认应使用结构化 `Modal` 承载风险提示、目标 `documentId` 展示与二次确认输入，不再使用轻量 `Popconfirm`
- 删除确认 `Modal` 的“确认删除”按钮在用户未精确匹配完整 `documentId` 前必须保持禁用
- 删除确认输入不匹配时，除禁用“确认删除”按钮外，还应显示明确错误提示，说明需输入完整且精确匹配的 `documentId`
- 删除确认输入错误提示只在用户开始输入后显示，`Modal` 初始打开时应先展示规则说明而不是错误状态
- 删除入口不再保留任意 `documentId` 手工删除模式，必须从已选定目标 `document` 的列表页或详情页进入删除确认
- 删除入口应同时保留在文档列表页和文档详情页，并共用同一套删除确认 `Modal` 规则
- 删除入口仅在文档状态为 `UPLOADED`、`INDEXED`、`FAILED` 时显示；对 `INGESTING`、`DELETING`、`DELETED` 不显示删除入口
- 删除入口仅对具备目标 `document` 管理权限的用户显示：`WORKSPACE_OWNER` / `WORKSPACE_ADMIN` 直接可见；存在文档级 `DOC_DENY` 时不可见；文档级 `DOC_ALLOW_MANAGE` 可见；文档无显式授权时回退到知识库 `KB_MANAGER`
- 删除成功后的结果提示应明确引导：如需继续使用该内容，需重新上传，并基于新 `document` 重新授权或继续后续操作
- 删除成功后的后续操作引导应由页面内稳定结果提示区域承载，不只依赖全局 `message.success`
- 删除成功后的页面内稳定结果提示应持续可见，直到用户主动关闭或离开当前页面
- 删除成功后的稳定结果提示应提供“去上传页”主按钮，直接承接重新上传动作
- 删除成功后点击“去上传页”时，不自动带入原文档所属 `knowledge base`，仍需用户手动选择目标 `knowledge base`
- 删除成功后的稳定结果提示应只读展示已删除的原 `documentId`，明确该提示对应的是旧 `document` 资产的收尾信息
- 删除成功后的稳定结果提示应明确说明：原 `documentId` 对应的文档级授权已随旧 `document` 资产结束，不适用于后续新上传文档
- 删除成功后的稳定结果提示应补充说明：知识库级角色不因旧 `document` 删除自动失效，避免将文档级授权结束误解为失去 `knowledge base` 访问权限
- 从文档详情页发起删除并成功后，前端应立即离开当前详情页并返回文档列表页，不继续停留在已删除 `document` 的详情上下文
- 从文档详情页删除返回文档列表页时，前端应尽量保留用户删除前的列表筛选条件与分页位置，而不是回到默认首屏
- 删除成功后，当前文档列表结果应立即移除该 `document`，不等待用户手动刷新
- 删除成功后若当前分页结果因此变为空页，前端应自动回退到上一页分页
- `document` 的版本历史应保留来源文件信息，便于审计与问题追踪
- 问答引用默认指向 `document` 的最新版本
- 当 `document` 存在更新版本时，UI 应显式提示“该文档已有更新”
- “该文档已有更新”提示应同时出现在列表页和详情页
- 当最新版本尚未 `INDEXED` 且问答仍使用旧已索引版本时，详情页与问答相关入口都应显式提示当前问答使用的不是最新版本
- 文档列表页默认展示的状态应跟随最新版本的真实状态更新；当新最新版本处于 `UPLOADED` / `INGESTING` 时，列表页直接显示该真实状态
- 当文档列表页中的新最新版本尚未 `INDEXED` 时，问答入口仍允许进入，但必须明确提示当前问答将使用最近一个已 `INDEXED` 的版本
- 当新最新版本处理失败时，列表页与详情页主视图仍落在失败的新最新版本，并显式提示问答仍使用最近一个已 `INDEXED` 的旧版本
- “上传新版本”仅在当前最新版本状态为 `INDEXED`、`FAILED` 时开放；对 `UPLOADED`、`INGESTING`、`DELETING`、`DELETED` 不开放
- 文档旧版本仅允许对目标 `document` 具备管理权限的用户通过直接访问查看
- 版本回退与删除是两类不同治理动作：删除结束整个 `document` 资产，版本回退用于改变哪个版本成为当前最新版本
- 版本回退应创建一个新的最新版本，其内容等同于被选中的历史版本，而不是直接改写既有版本链或回拨版本指针
- 版本回退仅对具备目标 `document` 管理权限的用户开放
- 版本回退入口只放在文档详情页的版本历史列表中，针对具体历史版本提供“回退为最新版本”动作
- 版本回退后端应对具体历史版本发起动作，而不是对整个 `document` 做模糊回退；接口语义应绑定目标历史版本，例如 `POST /api/v1/documents/{documentId}/versions/{versionNumber}/rollback`
- 版本回退接口请求体可为空，不额外要求业务字段；回退目标上下文由路径参数承载
- 版本回退接口也应携带 `expectedLatestVersionNumber` 一类乐观并发校验字段，用于防止用户基于过期版本视图发起回退
- 版本回退创建的新最新版本应在版本历史中明确标记为“回退产生的版本”
- 回退产生的新最新版本应明确显示其回退自哪个历史版本
- 回退产生的新最新版本在版本历史中应同时显示“最新”与“回退产生”标记
- 被选中作为回退目标的历史版本应标记为“曾被回退为最新版本”
- 曾被回退为最新版本的历史版本，后续仍允许再次作为版本回退目标
- 版本回退成功后，详情页应提供页面内稳定结果提示，而不只依赖瞬时成功消息
- 版本回退成功后的稳定结果提示应同时展示新最新版本号与回退目标版本号
- 版本回退成功后的稳定结果提示应提供“查看版本历史”快捷入口
- 版本回退成功后的稳定结果提示可提供“去问答”快捷入口，但前提是回退后的当前最新版本或当前可问答基线存在可问答版本
- “上传新版本”与“版本回退”的成功响应不应继续沿用仅含 `documentId` 与 `status` 的最小返回；应提供包含版本号上下文的专用结果 DTO
- “上传新版本”成功响应至少应返回 `documentId`、`versionNumber`、`previousVersionNumber`、`status`
- “版本回退”成功响应至少应返回 `documentId`、`versionNumber`、`rollbackFromVersionNumber`、`status`
- 版本治理相关成功响应应显式返回当前是否存在可问答版本的结果字段，例如 `canAskNow`
- 版本治理相关成功响应应返回 `latestVersionNumber`，显式表达系统当前认定的最新版本号
- 版本治理相关成功响应应返回 `askableVersionNumber`，用于明确当前问答基线实际落在哪个版本
- 版本治理相关成功响应应返回显式版本来源类型字段，例如 `versionOriginType = UPLOAD | ROLLBACK`
- “上传新版本”命中同内容复用时，成功结果应显式返回 `versionCreated = false`
- 同内容未创建新版本的成功结果应返回当前仍停留的版本号，例如 `reusedLatestVersionNumber`
- 版本治理相关成功结果应返回显式结果类型字段，例如 `versionResultType = CREATED | REUSED_IDENTICAL_CONTENT`
- 版本治理相关错误响应除 HTTP 状态与消息外，还应补充机器可判定的业务错误码
- “上传新版本”相关错误响应至少应覆盖：`VERSION_UPLOAD_NOT_ALLOWED_STATUS`、`VERSION_UPLOAD_NO_MANAGE_PERMISSION`、`VERSION_UPLOAD_DOCUMENT_NOT_FOUND`
- “版本回退”相关错误响应至少应覆盖：`VERSION_ROLLBACK_NOT_ALLOWED_STATUS`、`VERSION_ROLLBACK_NO_MANAGE_PERMISSION`、`VERSION_ROLLBACK_TARGET_NOT_INDEXED`、`VERSION_ROLLBACK_TARGET_IS_LATEST`、`VERSION_ROLLBACK_DOCUMENT_NOT_FOUND`、`VERSION_ROLLBACK_VERSION_NOT_FOUND`
- 版本治理相关错误响应应覆盖乐观并发校验失败场景，例如 `VERSION_CONFLICT_STALE_LATEST_VERSION`
- 同内容未创建新版本不应建模为错误响应，而应建模为成功结果中的专用分支
- 版本回退仅允许选择曾经成功形成可用内容的历史版本作为回退目标，即至少已 `INDEXED` 的版本
- 当前最新版本不允许再次被选择为“回退为最新版本”的目标
- 版本回退执行前应进行二次确认，明确提示该操作将改变当前最新版本
- 版本回退的二次确认不要求用户手动输入完整 `documentId`
- 版本回退确认中应明确提示：该操作会创建一个新的最新版本，而不是直接回拨旧版本指针
- 版本回退确认中应明确提示：问答基线可能随回退结果变化
- 版本回退创建的新最新版本初始状态应为 `UPLOADED`，并重新走处理链路
- 当回退产生的新最新版本尚未 `INDEXED` 时，问答默认仍回退到最近一个已 `INDEXED` 的版本
- 回退产生的新最新版本若最终处理 `FAILED`，仍保留其作为当前最新版本的语义
- 版本回退成功后的稳定结果提示应明确说明当前最新版本正在重新处理中
- 版本回退成功后，文档列表页状态列应立即显示回退产生的新最新版本真实状态，如 `UPLOADED` / `INGESTING`
- 当回退成功但新最新版本尚未 `INDEXED` 时，稳定结果提示应同时说明问答暂时仍使用最近一个已 `INDEXED` 的版本
- 普通用户直达旧版本时，系统应提示“该版本不可见，请查看最新版本”
- 文档列表中最新版本应同时展示版本号与“最新”标记
- 文档列表页与详情页默认展示的文档名称应跟随最新版本的来源文件名更新
- 当前阶段不为 `document` 引入独立于来源文件名的稳定标题字段
- 回退产生的新最新版本来源文件名应跟随被回退目标版本的来源文件名
- 文档详情页应展示当前版本号、总版本数与最新更新时间
- 上传新版本成功后，详情页应立即切换到新的最新版本，并明确展示 `UPLOADED` / `INGESTING` 等处理中状态
- 文档详情页应展示版本历史列表，并支持折叠
- 版本历史列表默认折叠
- 版本历史列表默认展示每个版本的来源文件名
- 版本历史列表默认展示上传时间和上传人
- 版本历史列表默认展示每个版本的中文状态文案
- 版本历史列表默认展示每个版本当前是否可用于问答
- 版本历史列表对 `FAILED` 版本默认展示简短失败原因摘要，其它状态不展示额外失败摘要
- 上传新版本成功后，版本历史列表应自动展开一次，便于用户立即核对新旧版本关系
- 用户切换查看历史版本后，版本历史列表应保持上一次展开状态，不自动重置为折叠
- 对目标 `document` 具备管理权限的用户可在详情页切换查看其他历史版本
- 切换到历史版本后，详情页主视图应显式提示当前正在查看历史版本，而不是最新版本
- 切换历史版本时应展示与最新版本的差异提示
- 差异提示默认采用摘要级展示，必要时可展开详细 diff
- 差异摘要至少包含新增内容、删除内容、修改内容三类信息
- 历史版本与最新版本的差异提示默认显示在详情页主视图顶部，而不是仅嵌在版本历史列表项内
- 查看历史版本不改变当前 `document` 的问答基线；问答默认仍围绕该 `document` 当前可问答的基线版本工作，不随历史版本查看临时切换
- 查看历史版本时，详情页主操作中的“重处理”仍默认作用于当前最新版本，而不是当前查看的历史版本
- 查看历史版本时，详情页主操作中的“删除”仍删除整个 `document` 资产，而不是当前历史版本
- 查看历史版本时，详情页应提供“返回最新版本”快捷入口
- 当最新版本处于 `FAILED` 时，详情页应提供“查看最近一个已 `INDEXED` 版本”或等价问答/查看快捷入口，但不将主视图自动回退到旧版本
- 失败版本经成功重处理后，应保持同一个版本号，仅更新该版本的处理状态，不因重处理成功额外生成新版本号
- 上传成功后前端默认跳转到文档详情页
- 文档详情页应提供重处理、删除、查看版本历史三个主操作入口
- 当最新版本处于 `FAILED` 时，详情页主操作中的“重处理”默认作用于当前失败的新最新版本
- 版本历史列表默认按最新版本在前排序
- 上传页在未选择 `knowledge base` 时应阻止提交并提示先选择知识库
- 切换 `knowledge base` 后，上传页已选文件应自动清空
- 每次进入上传页都不预选 `knowledge base`，必须手动选择
- 上传页文件拖拽区应显示当前选中的 `knowledge base` 名称
- 未选择 `knowledge base` 时，上传页应展示空态提示，并保留可交互的知识库选择入口
- 上传命中复用规则时，前端应明确提示该操作复用了既有 `document`
- 复用提示中应展示被复用 `document` 的可识别信息，便于直接跳转查看
- 复用提示默认展示文档名称与 `documentId`
- 点击复用提示后应直接跳转到文档详情页

### 5.3 document

`documentId` 表示 **文档资产 ID**，不是一次性的任务 ID。

- 版本信息应从当前单一 `document` 记录中拆出，形成 `document` 主表与 `document version` 子表的结构
- `document` 主表应保留当前最新版本的轻量投影，而不是继续承载全部版本级处理字段
- `document` 主表轻量投影至少应保留 `latestVersionNumber`
- `document` 主表轻量投影至少应保留 `latestStatus`
- `document` 主表轻量投影至少应保留 `latestFilename`
- 当前阶段 `document` 主表只保留 `latestVersionNumber` 作为版本头指针，不持久化 `askableVersionNumber`
- `document` 主表中的 `latestStatus` 应严格镜像 `latestVersionNumber` 指向版本的真实状态，而不是聚合态
- `document` 主表轻量投影应保留 `latestVersionOriginType`
- `document version` 应以 `documentId + versionNumber` 作为业务主键，必要时可增加内部 surrogate key
- `versionNumber` 应从 `1` 开始递增
- 版本回退产生的新版本也应占用下一个递增 `versionNumber`，而不是复用被回退目标的编号
- 同内容复用、未创建新版本时，不消耗新的 `versionNumber`
- `document version` 应持久化显式的 `versionOriginType` 字段，例如 `UPLOAD`、`ROLLBACK`
- `document version` 应持久化可空的 `rollbackFromVersionNumber` 字段，用于表达回退产生的新版本源自哪个历史版本
- `document version` 应持久化 `createdAt` 与 `updatedAt`
- `document version` 应持久化版本级创建人/上传人字段
- 回退操作者等更细粒度动作信息当前阶段优先通过审计事件查询，不额外在 `document version` 中单独持久化
- 当前阶段“可问答”能力仍由处理状态与问答基线规则推导，不在 `document version` 中单独持久化 `askable` / `askableAt` 字段
- 来源文件级字段如 `fileHash`、`filename`、`fileSize` 应下沉到 `document version`
- 处理结果级字段如 `status`、`failureReason`、`processingMetadata` 应下沉到 `document version`
- 重试与错误上下文字段如 `retryCount`、`retryMax`、`nextRetryAt`、`lastErrorCode`、`lastErrorMessage`、`lastErrorAt` 应下沉到 `document version`
- 长期看，重试与错误上下文字段还应继续从 `document version` 拆分到独立的处理尝试表，用于承载执行轨迹而非版本终态
- `processingMetadata` 当前阶段先保留在 `document version`
- `reprocessCount`、`reprocessRequestedAt` 更适合进入独立处理尝试表，而不是长期停留在 `document version`
- 同一未删除资产集合内，重复上传可复用既有 `documentId`
- 删除后允许重新上传，形成新的 `documentId`
- 不同内容是否归属同一 `document`，不能由系统仅凭文件名、上传人或路径等弱信号自动判断
- 删除后同内容重新上传形成的新 `document` 不自动继承旧 `documentId` 的文档级授权
- 删除确认必须明确告知：新 `document` 不自动继承旧 `documentId` 的文档级授权，需重新授权
- 删除确认主文案应聚焦高风险事实，不承载旧入口失效等可由用户推导的派生影响
- 每个 `document` 只归属于一个 `knowledge base`
- `reprocess` 只针对当前 `document` 资产重新执行处理，不创建新的 `document`

理解处理流程时，要区分：

- **文档资产**
- **处理过程**
- **向量化后的 chunk 集合**

不要把这三者混为一个对象。

### 5.4 chunk

chunk 是向量检索与引用展示的基本文本单元。

- 它来自文档解析与清洗后的内容
- 当前遵循“结构优先、长度兜底”的拆分策略
- chunk 是检索与引用单位，不等于原始段落，也不等于完整文档

### 5.5 processing metadata

`processingMetadata` / `processing_metadata` 是 **文档级处理结果元数据**，用于承载解析阶段产出的结构化结果。

不要把它理解成：

- 面向前端展示的业务主数据
- 最终 RAG 节点契约
- 任意扩展字段的杂物箱

它当前主要服务于处理终态追踪、调试和后续分块参考。

## 6. 关键状态语义

### 6.1 文档处理状态

当前文档资产生命周期使用以下状态：

- `UPLOADED`
- `INGESTING`
- `INDEXED`
- `FAILED`
- `DELETING`
- `DELETED`

删除语义采用软删除：保留 `documentId`、状态轨迹与审计上下文，删除后仍应能查询到该文档的终态信息。

这些状态是系统级事实，不要在讨论中自行发明近义状态名。

尤其注意：

- 上传接口返回的 `ACCEPTED` 是“请求已受理”的 API 语义，不是数据库主状态
- 删除成功后，状态查询返回 `DELETED`，而不是直接 `404`

### 6.2 权限相关角色

工作区角色：

- `WORKSPACE_OWNER`
- `WORKSPACE_ADMIN`
- `WORKSPACE_MEMBER`

知识库角色：

- `KB_MANAGER`
- `KB_CONTRIBUTOR`
- `KB_READER`
- `KB_ASKER`

文档覆盖权限：

- `DOC_ALLOW_READ`
- `DOC_ALLOW_MANAGE`
- `DOC_DENY`

讨论授权问题时，请使用这些权威枚举名，不要自造“编辑者”“浏览者”“超级管理员”等口语化替代词。

## 7. 当前架构共识

### 7.1 分阶段演进优先

这个仓库长期遵循的原则是：

- 先跑通端到端闭环
- 再做质量、治理和可扩展性增强

所以当你评估方案时，应该优先问：

- 是否守住主链路
- 是否保持边界清晰
- 是否为下一阶段演进留下接口

而不是默认要求“一次性做成最终形态”。

### 7.2 DDD-Lite 分层是当前组织方式

主要子域按下面的层次组织：

- `interfaces`
- `application`
- `domain`
- `infrastructure`

这是一种工程组织边界，不代表已经实现了完整教科书式 DDD。

在评审或设计时，应优先关注：

- 职责是否越层
- 应用层是否直接依赖具体 SDK / 存储细节
- 子域边界是否清晰

### 7.3 文档处理链路是 ingest 的核心复杂度来源

当前 ingest 不是单纯“上传文件到数据库”，而是包含一条真实的处理链路：

- 源文件读取
- 解析与清洗
- 中间产物落盘
- 文本分块
- 向量化
- 入库与状态收口

后续围绕解析质量、清洗策略、chunk 质量、OCR、多格式支持的讨论，都应被视为这条主链路的增强，而不是附属优化。

### 7.4 权限边界必须和问答边界一致

根据 `ADR-0005`，当前系统有一个非常重要的长期约束：

- `qa.ask` 只能使用当前用户有权访问的文档内容

不要接受这类设计：

- 检索阶段越权召回，展示阶段再“裁剪引用”
- 问答结果包含了用户本不该看到的内容，只是 references 被隐藏

对于 RAG 系统，这会直接破坏安全边界。

## 8. 当前技术与实现基线

从长期语义上，应把下面这些视为当前基线：

- 后端：Java 21 + Spring Boot
- AI 集成：Spring AI / Spring AI Alibaba
- Embedding / Chat：当前固定 DashScope 生态
- 向量存储：PostgreSQL + PGVector
- 处理模式：异步 worker（当前单进程）
- 前端：React + TypeScript + Vite

这意味着：

- 可以为未来切换 Provider、向量库和认证方式预留接口
- 但在没有明确 ADR 或计划支持时，不应默认把系统描述成“多 Provider 已完成”“多向量库已抽象到可随时替换”

## 9. 当前不应误判的边界

以下几件事在分析、设计和命名时容易被说错：

1. 当前系统不是“只有后端 demo”
现在已经有真实前端控制台、权限治理和管理能力。

2. 当前系统也不是“成熟企业知识平台”
很多能力仍处于单工作区、单主技术路线和逐步增强阶段。

3. `docs/` 才是工程事实真源
`deliverables/course/` 是课程交付层，`docs/learning/` 是学习沉淀层，不能倒置主从关系。

4. `reference/` 里的资料不是当前系统事实
它们是技术参考、对照资料，不应直接当成“系统已经这样实现了”。

5. 解析能力虽已升级，但 OCR 和复杂版式仍是增强项
不要在任何总结中把“复杂扫描件稳定支持”表述成既成事实。

## 10. 推荐阅读顺序

如果要快速恢复上下文，建议按这个顺序：

1. `README.md`
2. `docs/README.md`
3. `docs/01-product-scope.md`
4. `docs/02-roadmap.md`
5. `docs/03-architecture.md`
6. `docs/04-api-contract.yaml`
7. `docs/07-ingest-processing-execution.md`
8. `docs/adr/ADR-0001-v1-tech-baseline.md`
9. `docs/adr/ADR-0003-v1-dashscope-pgvector.md`
10. `docs/adr/ADR-0004-v1-ingest-processing-strategy.md`
11. `docs/adr/ADR-0005-rag-access-control-foundation.md`

## 11. 写作与评审约定

后续在这个仓库中撰写方案、评审设计或解释代码时，优先遵循这些约定：

- 使用仓库已有术语，不发明新的同义词
- 先区分“当前事实”“已采纳决策”“未来规划”
- 方案文档要明确 In Scope / Out of Scope
- 涉及架构取舍时，优先回看 ADR，而不是只看当前代码实现
- 涉及工作流或操作步骤时，优先落到 `docs/runbooks/`

## 12. 权威来源

以下内容默认优先级最高：

1. `docs/adr/` 中状态为 `Accepted` 的 ADR
2. `docs/` 下描述当前系统事实的主文档
3. `docs/runbooks/` 下的执行计划、完成概览和联调手册
4. 当前代码实现

当四者不一致时：

- 先判断是文档过期，还是实现偏离了已采纳决策
- 不要在未说明冲突的前提下，直接用某一方覆盖另一方

## 13. 后续演进

本文件应该随着仓库主语义变化而更新，尤其是以下情况：

- 新增稳定子域
- 核心资源模型变化
- 权限模型变化
- 多工作区 / 多租户语义正式落地
- `docs/` 的主导航结构发生迁移

如果只是某个局部实现细节变化，不要把它直接堆进本文件；优先放到对应的设计文档、ADR 或 runbook 中。
