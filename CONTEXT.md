# my-AI 仓库上下文

## 1. 文档目的

本文件用于说明 `my-AI` 仓库当前稳定的业务语义、系统边界、核心术语和决策约束。

目标读者：

- 未来重新接手这个仓库的自己
- 参与该仓库的协作者
- 需要先理解领域语言再开始分析或改造代码的 agent / tooling

本文件不替代详细设计、运行手册或版本计划；它只描述“这个仓库长期成立的上下文”。

## 2. 仓库定位

`my-AI` 是一个基于 Spring Boot + Spring AI 的文档入库与检索问答项目。

当前主线目标不是一次性做成完整 SaaS 平台，而是按阶段演进，先把下面这条链路做稳：

`上传文档 -> 可追踪处理 -> 可检索 -> 可问答 -> 可治理`

当前仓库已经从早期的最小 RAG 闭环，演进到包含以下能力的单工作区系统：

- 文档上传、处理、重处理、删除
- 知识库主数据管理
- 基于知识库的问答
- 单工作区下的认证、授权和审计基础能力
- 前后端一体的本地演示与联调链路

## 3. 当前阶段

当前应把本仓库理解为：

- **单工作区优先** 的内部协作型系统
- **RAG 主链路已跑通**，纯文字解析/清洗第一轮优化已落地，复杂文档解析增强已形成 Docling 适配器决策，后续继续增强图片、表格、OCR、治理能力和工程稳定性
- **V1.1 管理基线已形成**，后续重点会继续落在解析增强、权限完善和更真实的业务场景支持上

不要把它理解成：

- 已完成多租户 SaaS 的成熟产品
- 已稳定支持所有复杂文档格式和 OCR 场景
- 已完成多模型、多供应商自由切换的平台

## 4. 领域主线

本仓库围绕三条主业务线组织：

### 4.1 ingest

负责文档资产的生命周期管理：

- 上传受理
- 状态追踪
- 解析与清洗
- 分块
- 向量入库
- 重处理
- 删除

这里的核心关注点是”文档能否被稳定处理并形成可追踪资产”，而不是回答生成本身。

处理主链细节（文件类型路由、cleaned.md 契约、正文读取、存储策略、Docling 采纳决策）见：
- `docs/features/ingest/SPEC.md`
- `docs/adr/ADR-0004-v1-ingest-processing-strategy.md`
- `docs/adr/ADR-0007-s3-compatible-document-asset-storage.md`
- `docs/adr/ADR-0008-docling-complex-document-parser-adapter.md`

### 4.2 knowledge

负责知识库主数据和可见性边界：

- 知识库创建、编辑、启停
- 知识库列表与统计视图
- 知识库作为上传与问答的主要业务边界

知识库是当前系统中最稳定、最自然的资源隔离单位。

### 4.3 qa

负责问答编排：

- 检索候选内容
- 构建问答输入
- 调用模型生成回答
- 返回结构化引用

`qa` 的职责不是拥有文档，而是消费 `ingest` 产出的可检索内容，并受 `knowledge` 和授权规则约束。

问答基线、引用字段规范与 stale reference 行为规则见 `docs/features/qa/SPEC.md`。

### 4.4 auth / governance

当前仓库已经有独立的认证与治理基础能力，虽然 README 仍以 `ingest / knowledge / qa` 为主线介绍，但分析现状时必须把认证和授权视为一等能力，而不是外围补丁：

- 本地账号登录
- 服务端 Session
- 工作区成员治理
- 知识库授权
- 文档授权覆盖
- 审计留痕

## 5. 核心资源与标识

理解本仓库时，优先使用下面这些术语，不要随意替换成语义接近但边界不同的词。

### 5.1 workspace

当前系统按 **单工作区** 优先设计，但 `workspace` 已经入模。

- 现在不要把它理解为“未来才会有的概念”
- 也不要把整个系统简单等同于“无边界的全局空间”
- `workspace` 是底层治理单元，主要承载成员归属、权限范围、审计范围与配置范围
- 普通用户在 UI 中只感知自己当前所属的 `workspace`，不需要看到其他 `workspace`
- 普通用户不能直接修改 `workspace`，`workspace` 相关变更只应由治理路径处理
- 当前用户模型按单归属处理：一个用户只隶属于一个 `workspace`

### 5.2 knowledge base

知识库是当前最主要的业务资源边界。

- 对外稳定标识是 `kbId`
- 文档上传、列表、问答、授权大多围绕知识库展开
- 不要把 knowledge base 混叫成”目录””分组””项目空间”
- 每个 `knowledge base` 只隶属于一个 `workspace`
- 上传文档时必须先选定 `knowledge base`，再创建或复用对应的 `document`
- 同一 `knowledge base` 下，同一内容哈希的上传应复用既有 `documentId`，文件名变化不影响是否复用

知识库相关的版本治理、上传新版本、版本回退、删除、正文读取与版本历史交互规则见 `docs/features/document-versioning/SPEC.md`。

### 5.3 document

`documentId` 表示 **文档资产 ID**，不是一次性的任务 ID。

- `document` 主表保留当前最新版本的轻量投影（`latestVersionNumber`、`latestStatus`、`latestFilename`、`latestVersionOriginType`），完整版本事实由 `document version` 子表承载
- `document version` 以 `documentId + versionNumber` 作为业务主键，`versionNumber` 从 1 递增
- 每个 `document` 只归属于一个 `knowledge base`
- `reprocess` 只针对当前 `document` 资产重新执行处理，不创建新 `document`

理解处理流程时，要区分：
- **文档资产**
- **处理过程**
- **向量化后的 chunk 集合**
不要把这三者混为一个对象。

数据模型细节（主表投影字段、版本表结构、字段下沉决策）见 `docs/features/document-versioning/SPEC.md`。

### 5.4 chunk

chunk 是向量检索与引用展示的基本文本单元。

- 它来自文档解析与清洗后的内容
- 当前遵循“结构优先、长度兜底”的拆分策略
- 当前实现先将 `cleaned.md` 切成结构片段，再按空白分隔 token 组装窗口；配置名仍是 `chunkSize` / `overlapSize`
- `sourceHint` 当前主要承载标题上下文 JSON，用于解释 chunk 来源，不是完整节点元数据契约
- chunk 是检索与引用单位，不等于原始段落，也不等于完整文档
- `documents/chunks/preview` 是当前观察 chunk 质量和解析/清洗结果是否可用的主要验证面

### 5.5 processing metadata

`processingMetadata` / `processing_metadata` 是 **文档级处理结果元数据**，用于承载解析阶段产出的结构化结果。

不要把它理解成：

- 面向前端展示的业务主数据
- 最终 RAG 节点契约
- 任意扩展字段的杂物箱

它当前主要服务于处理终态追踪、调试和后续分块参考。

当前 `ingest` 主链中的正式中间文本产物是 `cleaned.md`，而不是 `processingMetadata` 本身。

当前 `processingMetadata` 的基础结构由 parser 生成，并在 `INDEXED` / `FAILED` 终态回填，主要包含：

- `schema_version`
- `stable.source_file`
- `stable.file_ext`
- `stable.mime_type`
- `stable.quality`
- `stable.created_at`
- 条件性 `language`、`page_count`、`primary_title`、`title_outline_sample`

## 6. 关键状态语义

### 6.1 文档处理状态

当前文档资产生命周期使用以下状态：

- `UPLOADED`
- `INGESTING`
- `INDEXED`
- `FAILED`
- `DELETING`
- `DELETED`

删除语义采用软删除：保留 `documentId`、状态轨迹与审计上下文。

这些状态是系统级事实，不要在讨论中自行发明近义状态名。

上传接口返回的 `ACCEPTED` 是”请求已受理”的 API 语义，不是数据库主状态。
删除成功后，状态查询返回 `DELETED`，而不是直接 `404`。

### 6.2 权限相关角色

当前授权模型分三层：

- 工作区角色决定用户是否拥有工作区级治理能力
- 知识库角色决定成员在某个 `knowledge base` 内的默认能力
- 文档覆盖权限用于处理少量文档级例外

授权判断优先级：

1. `WORKSPACE_OWNER` / `WORKSPACE_ADMIN` 拥有工作区内全量治理与资源访问能力
2. `WORKSPACE_MEMBER` 必须继续命中知识库角色或文档覆盖权限
3. `DOC_DENY` 用于拒绝普通成员访问特定文档
4. 文档没有覆盖权限时，回退到知识库角色判断

工作区角色：

- `WORKSPACE_OWNER`：工作区最终负责人，拥有成员治理、授权治理、审计查看和所有资源访问能力
- `WORKSPACE_ADMIN`：工作区管理员，作为 owner 的治理代理，拥有工作区内全量管理能力
- `WORKSPACE_MEMBER`：普通工作区成员，不默认拥有全部知识库或文档访问权，必须通过知识库角色或文档覆盖权限获得资源访问能力

知识库角色：

- `KB_MANAGER`：知识库级最高角色，可管理知识库配置、成员授权、文档治理、版本历史、回退、删除、正文查看与问答
- `KB_CONTRIBUTOR`：内容贡献角色，可上传、上传新版本、重处理、查看正文与问答，但不可管理授权、删除知识库或执行仅管理者可做的治理动作
- `KB_READER`：普通只读角色，可问答、查看文档列表、查看问答基线版本正文和问答引用侧栏，不可上传、重处理、删除、回退、授权或浏览任意历史版本
- `KB_ASKER`：废弃角色，不再作为新授权选项；后续迁移中应收敛到 `KB_READER` 或按兼容策略处理既有数据

文档覆盖权限：

- `DOC_ALLOW_READ`：对单个 `document` 的只读例外授权，可让成员读取该文档当前可见正文和参与问答
- `DOC_ALLOW_MANAGE`：对单个 `document` 的管理例外授权，可让成员管理该文档版本、重处理、回退、删除和查看历史版本，但不授予整个知识库的授权治理能力
- `DOC_DENY`：对单个 `document` 的拒绝覆盖，用于从知识库授权范围中排除敏感文档

讨论授权问题时，请使用这些权威枚举名，不要自造“编辑者”“浏览者”“超级管理员”等口语化替代词。

## 7. 当前架构共识

### 7.1 分阶段演进优先

这个仓库长期遵循的原则是：

- 先跑通端到端闭环
- 再做质量、治理和可扩展性增强

所以当你评估方案时，应该优先问：

- 是否守住主链路
- 是否保持边界清晰
- 是否为下一阶段演进留下接口

而不是默认要求“一次性做成最终形态”。

### 7.2 DDD-Lite 分层是当前组织方式

主要子域按下面的层次组织：

- `interfaces`
- `application`
- `domain`
- `infrastructure`

这是一种工程组织边界，不代表已经实现了完整教科书式 DDD。

在评审或设计时，应优先关注：

- 职责是否越层
- 应用层是否直接依赖具体 SDK / 存储细节
- 子域边界是否清晰

### 7.3 文档处理链路是 ingest 的核心复杂度来源

当前 ingest 不是单纯“上传文件到数据库”，而是包含一条真实的处理链路：

- 源文件读取
- 文件类型路由
- 解析与清洗，生成 `cleaned.md`
- 中间产物落盘
- 文本分块
- 向量化
- 入库与状态收口

其中当前主链的正式中间文本产物是 `cleaned.md`；围绕解析质量、清洗策略和现有 chunk 行为的优化，默认都应先回到这份文本产物本身判断是否变好。

纯文字阶段已经形成固定黄金样本与回归闭环：`weak-pdf-001 -> md-001 -> md-002 -> html-001 -> word-001`。后续围绕解析质量、清洗策略、chunk 质量、OCR、多格式支持、图片理解、表格结构化的讨论，都应被视为这条主链路的增强，而不是附属优化。

### 7.4 权限边界必须和问答边界一致

根据 `ADR-0005`，当前系统有一个非常重要的长期约束：

- `qa.ask` 只能使用当前用户有权访问的文档内容

不要接受这类设计：

- 检索阶段越权召回，展示阶段再“裁剪引用”
- 问答结果包含了用户本不该看到的内容，只是 references 被隐藏

对于 RAG 系统，这会直接破坏安全边界。

前端控制台的角色体验分层、页面主叙事、共享骨架原则与视角切换规则见 `docs/features/frontend-console/UX_SPEC.md`。

视觉与组件实现约束遵循根目录 `DESIGN.md`（Unified RAG Design System）。

## 8. 当前技术与实现基线

从长期语义上，应把下面这些视为当前基线：

- 后端：Java 21 + Spring Boot
- AI 集成：Spring AI / Spring AI Alibaba
- Embedding / Chat：当前固定 DashScope 生态
- 向量存储：PostgreSQL + PGVector
- 处理模式：异步 worker（当前单进程）
- 前端：React + TypeScript + Vite

这意味着：

- 可以为未来切换 Provider、向量库和认证方式预留接口
- 但在没有明确 ADR 或计划支持时，不应默认把系统描述成“多 Provider 已完成”“多向量库已抽象到可随时替换”

## 9. 当前不应误判的边界

以下几件事在分析、设计和命名时容易被说错：

1. 当前系统不是“只有后端 demo”
现在已经有真实前端控制台、权限治理和管理能力。

2. 当前系统也不是“成熟企业知识平台”
很多能力仍处于单工作区、单主技术路线和逐步增强阶段。

3. `docs/` 才是工程事实真源
`deliverables/course/` 是课程交付层，`docs/learning/` 是学习沉淀层，不能倒置主从关系。

4. `reference/` 里的资料不是当前系统事实
它们是技术参考、对照资料，不应直接当成“系统已经这样实现了”。

5. 解析能力虽已升级，且 Docling 复杂文档解析增强已被采纳，但当前实现仍未稳定支持图片理解、复杂表格节点和复杂扫描件
不要在任何总结中把“Docling 已实现”“图片理解已完成”“复杂表格节点已完成”或“复杂扫描件已稳定支持”表述成既成事实。

## 10. 写作与评审约定

后续在这个仓库中撰写方案、评审设计或解释代码时，优先遵循这些约定：

- 使用仓库已有术语，不发明新的同义词
- 先区分“当前事实”“已采纳决策”“未来规划”
- 方案文档要明确 In Scope / Out of Scope
- 涉及架构取舍时，优先回看 ADR，而不是只看当前代码实现
- 涉及工作流或操作步骤时，优先落到 `docs/runbooks/`
- 评审解析、清洗与分块质量时，优先基于固定样本、`documents/chunks/preview` 和问答回归，而不是只凭主观印象

## 11. 权威来源

以下内容默认优先级最高：

1. `docs/adr/` 中状态为 `Accepted` 的 ADR
2. `docs/` 下描述当前系统事实的主文档
3. `docs/runbooks/` 下的执行计划、完成概览和联调手册
4. 当前代码实现

当四者不一致时：

- 先判断是文档过期，还是实现偏离了已采纳决策
- 不要在未说明冲突的前提下，直接用某一方覆盖另一方

## 12. 后续演进

本文件应该随着仓库主语义变化而更新，尤其是以下情况：

- 新增稳定子域
- 核心资源模型变化
- 权限模型变化
- 多工作区 / 多租户语义正式落地

如果只是某个局部实现细节变化，不要把它直接堆进本文件；优先放到对应的 SPEC、ADR 或 runbook 中。

特别约束：API 契约、UI 交互规则、错误码枚举、测试用例清单、DTO 字段定义不属于本文件的内容范围。这些应落在 `docs/features/<feature>/SPEC.md`、`docs/adr/` 或 `docs/architecture/` 中。
